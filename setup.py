#!/usr/bin/env python3
"""Minimal setup stub — all metadata lives in pyproject.toml (PEP 621).

This file exists only for legacy compatibility (e.g., `python setup.py develop`).
Use `uv sync --frozen` or `pip install -e .` for normal installation.
"""
from setuptools import setup

setup()
