# app-ci.yml — PR-triggered pipeline for the production app
#
# Runs: flake8 (lint) → mypy (type check) → bandit (security scan)
#       → pytest unit tests + 90% coverage gate → integration tests
#
# Idempotent: all steps are stateless and safe to re-run.
# Python: 3.11+ (per setup.py python_requires=">=3.11")
# Ubuntu-latest ships SQLite ≥ 3.38.0 (ADR-004 requirement satisfied).
#
# Excludes lab/ paths — those are covered by lab-ci.yml.

name: App CI

on:
  push:
    branches: ["**"]
    paths-ignore:
      - "lab/**"
      - "docs/**"
      - "checkpoints/**"
      - "*.md"
      - "INSTALL.md"
      - "README.md"
  pull_request:
    paths-ignore:
      - "lab/**"
      - "docs/**"
      - "checkpoints/**"
      - "*.md"

jobs:
  lint:
    name: Lint & Static Analysis
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install uv
        run: pip install uv

      - name: Install dev dependencies
        run: |
          uv sync --frozen
          echo "$PWD/.venv/bin" >> $GITHUB_PATH

      - name: Lint (ruff)
        run: |
          ruff check . \
            --select=F401,F811,F541,F841,E9,W605 \
            --line-length=120 \
            --exclude=venv,pigskin_auction_draft.egg-info,lab,.agents

      - name: Type check (mypy)
        run: mypy . --ignore-missing-imports --exclude venv --exclude lab

      - name: Security scan (bandit — fail on MEDIUM+ severity)
        run: |
          bandit -r . -ll \
            --exclude ./venv,./.venv,./tests,./pigskin_auction_draft.egg-info,./lab

  test:
    name: Tests & Coverage Gate
    runs-on: ubuntu-latest
    needs: lint
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install uv
        run: pip install uv

      - name: Install dev dependencies
        run: |
          uv sync --frozen
          echo "$PWD/.venv/bin" >> $GITHUB_PATH

      - name: Unit tests + coverage gate (≥ 90%)
        run: |
          pytest tests/ -x -q --timeout=60 \
            --ignore=tests/property \
            --cov=. --cov-fail-under=90 \
            --cov-report=xml

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report-${{ github.run_id }}
          path: coverage.xml
          retention-days: 14

      - name: Integration tests
        run: pytest tests/test_integration.py -v --timeout=120
